function globach(shu) {
    let globach = document.getElementById('globach')
    let num1 = parseInt(document.getElementById('num1').value)
    if (isNaN(odd)) {
        alert('请输入数字')
        return false
    }
    globach.value = func(odd)
}